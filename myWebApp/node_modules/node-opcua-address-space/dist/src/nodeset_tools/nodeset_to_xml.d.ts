import { BaseNode, INamespace } from "node-opcua-address-space-base";
import { ITranslationTable } from "../../source/xml_writer";
export declare function constructNamespaceTranslationTable(dependency: INamespace[]): ITranslationTable;
export declare function sortByBrowseName(x: BaseNode, y: BaseNode): number;
declare type NodeIdString = string;
export interface BuildAliasesData {
    aliases: Record<string, NodeIdString>;
    aliases_visited?: Record<string, unknown>;
}
export interface DumpXMLOptions {
}
export {};
